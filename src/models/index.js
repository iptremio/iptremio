const { Stream } = require('./stream');
const { Metadata } = require('./metadata');
const { Program } = require('./program');

/**
 * Stream model
 * Represents a media stream with various properties like id, type, name, etc.
 */
module.exports = {
    Stream,
    Metadata,
    Program,
};
